"use client";

export default function Navbar() {
  return (
    <nav className="glass fixed top-0 left-0 right-0 z-50 h-16 flex items-center px-8">
      <span className="font-space-grotesk font-semibold text-lg" style={{ fontFamily: "var(--font-space-grotesk)" }}>
        ST
      </span>
    </nav>
  );
}
